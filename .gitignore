# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.test
.env.local
# Note: deployment/.env.production is a TEMPLATE (allowed)
# But actual production .env files with real credentials should be ignored:
cp2b_web/backend/.env.production

# Deployment secrets (actual .env files with real credentials)
/var/www/cp2b/backend/.env
db-credentials.txt

# Backups
deployment/backups/
*.backup
*.sql.gz
*.tar.gz

# Production uploads
/var/www/cp2b/backend/uploads/

# Python (extract-strategic-data.py is run locally, ad hoc)
__pycache__/
*.pyc

# Temporary files
*.old.*

# Source material from Luciana's data drop (spreadsheets, reports, photos,
# ~1.4GB of building renders/videos) — archived locally, never committed.
# See PREDIO_CP2B_ARCHIVE.md for the inventory and what to do with it.
/New_Data_To_Update_Website/
