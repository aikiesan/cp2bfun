# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.test
.env.local
# Note: deployment/.env.production is a TEMPLATE (allowed)
# But actual production .env files with real credentials should be ignored:
cp2b_web/backend/.env.production

# Deployment secrets (actual .env files with real credentials)
/var/www/cp2b/backend/.env
db-credentials.txt

# Backups
deployment/backups/
*.backup
*.sql.gz
*.tar.gz

# Production uploads
/var/www/cp2b/backend/uploads/

# Python (extract-strategic-data.py is run locally, ad hoc)
__pycache__/
*.pyc

# Temporary files
*.old.*

# Source material and local data drops — archived locally, never committed to git.
# See PREDIO_CP2B_ARCHIVE.md for the inventory and what to do with it.
/New_Data_To_Update_Website/
/raw_materials/
/local_source_files/
/assets_archive/

# Prevent accidental drops of loose presentations, reports and raw binaries in root
/*.pdf
/*.pptx
/*.ppt
/*.docx
/*.xlsx
/*.skp

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

