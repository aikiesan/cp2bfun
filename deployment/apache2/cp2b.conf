<VirtualHost *:80>
    ServerName cp2b.unicamp.br
    ServerAlias www.cp2b.unicamp.br

    # Admin contact
    ServerAdmin admin@cp2b.unicamp.br

    # Document root for React frontend
    DocumentRoot /var/www/cp2b/repo/cp2b_web/dist

    # Frontend directory configuration
    <Directory /var/www/cp2b/repo/cp2b_web/dist>
        Options -Indexes +FollowSymLinks
        AllowOverride None
        Require all granted

        # Enable SPA routing - redirect all requests to index.html
        # EXCEPT for API calls and uploaded files
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteCond %{REQUEST_URI} !^/api/
        RewriteCond %{REQUEST_URI} !^/uploads/
        RewriteCond %{REQUEST_URI} !^/pilar2b
        RewriteRule . /index.html [L]
    </Directory>

    # PILAR-2b API
    ProxyPass /pilar2b/api/ http://127.0.0.1:8001/api/
    ProxyPassReverse /pilar2b/api/ http://127.0.0.1:8001/api/

    # Next.js Static & Image Optimization
    ProxyPass /pilar2b/_next/ http://127.0.0.1:3002/pilar2b/_next/
    ProxyPassReverse /pilar2b/_next/ http://127.0.0.1:3002/pilar2b/_next/

    # Next.js App
    ProxyPass /pilar2b http://127.0.0.1:3002/pilar2b
    ProxyPassReverse /pilar2b http://127.0.0.1:3002/pilar2b

    # Reverse proxy for backend API
    ProxyPreserveHost On
    ProxyPass /api http://localhost:3001/api
    ProxyPassReverse /api http://localhost:3001/api

    # Reverse proxy for uploaded files
    ProxyPass /uploads http://localhost:3001/uploads
    ProxyPassReverse /uploads http://localhost:3001/uploads

    # Security headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"

    # Cache control for static assets
    <FilesMatch "\.(ico|jpg|jpeg|png|gif|svg|webp|woff|woff2|ttf|eot|css|js)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>

    # Don't cache HTML files
    <FilesMatch "\.(html)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>

    # Enable gzip compression
    <IfModule mod_deflate.c>
        AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css
        AddOutputFilterByType DEFLATE application/javascript application/json
        AddOutputFilterByType DEFLATE application/xml application/xhtml+xml
        AddOutputFilterByType DEFLATE image/svg+xml
    </IfModule>

    # Logs
    ErrorLog ${APACHE_LOG_DIR}/cp2b-error.log
    CustomLog ${APACHE_LOG_DIR}/cp2b-access.log combined

    # Optional: Set log level (error, warn, info, debug)
    LogLevel warn
</VirtualHost>
